<odoo>
    <data noupdate="1">

        <record id="ir_cron_update_interest_daily" model="ir.cron">
            <field name="name">Cap nhat lai hang ngay</field>
            <field name="model_id" search="[('model', '=', 'loan.contract')]"/>
            <field name="state">code</field>
            <field name="code">model._cron_update_interest_daily()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
